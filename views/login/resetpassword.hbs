<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restablecer Contraseña</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Lato', sans-serif;
            background: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            flex-direction: column;
        }

        .login-wrap {
            background: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            margin-bottom: 20px;
        }

        .login-wrap .icon {
            font-size: 40px;
            color: #40055d;
            margin-bottom: 20px;
            text-align: center;
        }

        .login-wrap h3 {
            font-size: 24px;
            margin-bottom: 20px;
            font-weight: 400;
            text-align: center;
            color: #40055d;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-control {
            height: 50px;
            background: #e9ecef;
            border: none;
            padding-left: 20px;
            font-size: 16px;
        }

        .btn {
            height: 50px;
            background: #5e056b;
            border: none;
            width: 100%;
            font-size: 16px;
            border-radius: 5px;
        }

        .btn:hover {
            background: #9a60a4;
        }
    </style>
</head>
<body>
    <div class="login-wrap p-4 p-md-5">
        <div class="icon d-flex align-items-center justify-content-center">
            <span class="fa fa-lock"></span>
        </div>
        <h3 class="heading-section">Restablecer Contraseña</h3>
        <form action="/resetpassword/{{token}}" method="post">
            <div class="form-group">
                <input type="password" id="password" name="password" class="form-control rounded-left" placeholder="Nueva Contraseña" required>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary rounded submit p-3 px-5">Restablecer</button>
            </div>
        </form>
    </div>
    {{#if flashMessages.error}}
    <script>
        Swal.fire({
            title: 'Error',
            text: '{{flashMessages.error}}',
            icon: 'error',
            confirmButtonText: 'OK'
        });
    </script>
    {{/if}}

    {{#if flashMessages.success}}
    <script>
        Swal.fire({
            title: 'Success',
            text: '{{flashMessages.success}}',
            icon: 'success',
            confirmButtonText: 'OK'
        });
    </script>
    {{/if}}
</body>
</html>
